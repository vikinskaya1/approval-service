# SQLite (default, easiest for local dev):
DATABASE_URL=sqlite:///./approval_service.db

# Or Postgres (matches docker-compose.yml):
# DATABASE_URL=postgresql+psycopg2://approval:approval@localhost:5432/approval_service

APP_ENV=local
LOG_LEVEL=INFO
